import 'package:flutter/material.dart';

class SingUpContainerBody extends StatelessWidget {
  const SingUpContainerBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
            
      ],
    );
  }
}